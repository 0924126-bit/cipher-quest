/// Non-web stub for PWA detection. Always reports "not a PWA".
bool detectPwaMode() => false;

bool detectInstalledDisplay() => false;

void saveLastRoute(String path) {}

String? loadLastRoute() => null;
